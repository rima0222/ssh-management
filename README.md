# 🚀 پنل مدیریت SSH (نصب سریع)

برای نصب کامل (پیش‌نیازها + پنل) کد زیر را کپی کرده و در سرور اجرا کنید:

```bash
apt update && apt install -y curl && curl -Ls https://raw.githubusercontent.com/rima0222/ssh-management/main/install.sh
