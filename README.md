
# SSH Pro Management 🚀

برای نصب سریع پنل، دستور زیر را کپی و در سرور لینوکس وارد کنیدsudo apt update && sudo apt install -y python3 python3-flask bc vnstat curl && ufw allow 5000/tcp && ufw allow 443/tcp:

```bash
curl -Ls https://raw.githubusercontent.com/rima0222/ssh-management/main/install.sh
